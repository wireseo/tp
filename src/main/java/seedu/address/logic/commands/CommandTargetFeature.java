package seedu.address.logic.commands;

/**
 * Enum for the Command type. This enum is useful for identifying the type of Command Object executed.
 */
public enum CommandTargetFeature {
    NotAssigned,
    Students,
    Missions,
    Quest,
    Consultations,
    MasteryCheck,
    Tasks,
}
